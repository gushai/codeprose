package org.codeprose.api

import org.codeprose.util.DynamicPropertyMap

/**
 * Language independent summary class which can be tailored to  
 * a project's and Provider's properties via the DynamicPropertyMap.  
 */
class ProjectSummary() extends DynamicPropertyMap